# Full SEO Suite Implementation Plan

> **For agentic workers:** Implement task-by-task. Steps use checkbox syntax.

**Goal:** Ship storefront-native SEO (meta/schema/sitemap) plus admin SEO hub with SERP preview and scores.

**Architecture:** `includes/seo.php` + `ensure_seo_schema()`; fields on products/categories/blog; admin `seo.php` hub; storefront pages consume helper before `header.php`.

**Tech Stack:** PHP, SQLite/MySQL, existing admin Tailwind layout.

## Global Constraints

- No third-party SEO SaaS
- Keep checkout/account/cart `noindex`
- Pretty URLs via existing `url()` / router
- Focus keyword optional (scoring only)

---

### Task 1: Schema + SEO helper
- [ ] `ensure_seo_schema()` columns on products, categories, blog_posts
- [ ] `includes/seo.php`: title pattern, score, breadcrumbs, product/FAQ JSON-LD, apply helpers
- [ ] Require from bootstrap; call ensure from `db()`

### Task 2: Storefront wiring
- [ ] Product page: SEO overrides, FAQ UI + schema, breadcrumbs, image alt
- [ ] Shop: category-aware title/desc/H1/intro
- [ ] Blog post: SEO overrides
- [ ] Policy canonicals to clean paths
- [ ] Header: `og:image:alt`; Organization address if settings present
- [ ] Product card alt text

### Task 3: Sitemap enrichment
- [ ] Static pages + categories + image nodes + lastmod

### Task 4: Admin editors
- [ ] Product edit SEO section + FAQ repeater + SERP preview
- [ ] Categories SEO + intro fields

### Task 5: Admin SEO hub
- [ ] `admin/seo.php` defaults, checklist, inventory tabs, sitemap copy
- [ ] Nav link near Settings

### Task 6: Smoke-check
- [ ] `/`, `/shop`, product, `/sitemap.xml`, `/admin/seo.php` PHP lint + curl
